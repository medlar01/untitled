server:
  port: 9101
graphql:
  servlet:
    mapping: /graphql
    corsEnabled: false
    enabled: true
  tools:
    schema-location-pattern: "schema/*.graphqls"
spring:
  application:
    name: consul-provider
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
