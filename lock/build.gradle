plugins {
    id 'java'
    id "com.google.protobuf" version "0.8.9"
}

def library = rootProject
        .ext
        .dependency

group 'com.zbc.lock'
version '1.0'

dependencies {
    implementation library['curator-framework']
}